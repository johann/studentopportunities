<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title><%= content_for?(:title) ? yield(:title) : "Student Opportunities" %></title>
    <meta name="description" content="<%= content_for?(:description) ? yield(:description) : "Student Opportunities" %>">
    <%= stylesheet_link_tag "application", :media => "all" %>
    <%= javascript_include_tag "application" %>
    <%= csrf_meta_tags %>
    <%= yield(:head) %>
  </head>
  <body class="<%= controller_name %> <%= action_name %>">
    <header class="navbar navbar-fixed-top navbar-inverse">
      <nav class="navbar-inner">
        <div class="container">
          <%= render 'layouts/navigation' %>

        </div>
      </nav>
    </header>
    <div id="main" role="main">
      <div class="">
        <div class="container">
        <!--
           <div class="row">

            <div class="span3 sidebar">
              <ul class="nav nav-tabs nav-stacked">
                <li class="active"><%= link_to 'Student Organizations', root_url %></li>
                <li><%= link_to 'Greek Organizations', greek_organizations_path %></li>
                <li class=""><%= link_to 'Academic Honor Societies', academic_honor_societies_path %></li>
                <li class="disabled"><%= link_to 'University Honor Societies' %></li>
                <li class="disabled"><%= link_to 'University Programs' %></li>
                <li class="disabled"><%= link_to 'Graduate Assistantships' %></li>
                <li class="disabled"><%= link_to 'Scholarships/Grants' %></li>
                <li class="disabled"><%= link_to 'Job Opportunities' %></li>
                <li class="disabled"><%= link_to 'Internships' %></li>
                <li class="disabled"><%= link_to 'Other' %></li>
              </ul>
              

            </div>
          -->
            <div class="span12">
              <%= render 'layouts/messages' %>
              <%= yield %>
            </div>
          <footer>
          </footer>
        </div>
      </div> <!--! end of .container -->
    </div> <!--! end of #main -->
  </body>
</html>
